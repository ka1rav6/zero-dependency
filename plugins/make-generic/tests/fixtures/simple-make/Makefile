# A fixture Makefile. `kap plugin test` never runs make — it only checks the
# argv arrays the plugin produces — so the recipes here are just plausible.
all:
	@echo building

test:
	@echo testing

clean:
	rm -rf out
