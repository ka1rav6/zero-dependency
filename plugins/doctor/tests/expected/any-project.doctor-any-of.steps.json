{
  "fixture": "any-project",
  "command": "doctor",
  "config": {
    "matched_plugins": ["ports"],
    "required_tools": ["ss,lsof,netstat"],
    "optional_tools": []
  },
  "tools": ["lsof"],
  "steps": [
    { "cmd": ["echo", "kap doctor"] },
    { "cmd": ["echo", "  plugin   ports"] },
    { "cmd": ["echo", "  ok       lsof"] },
    { "cmd": ["echo", "healthy"] }
  ]
}
