{
  "fixture": "any-project",
  "command": "doctor",
  "config": {
    "matched_plugins": ["cargo-rust"],
    "required_tools": ["cargo"],
    "optional_tools": []
  },
  "tools": [],
  "steps": [
    { "cmd": ["echo", "kap doctor"] },
    { "cmd": ["echo", "  plugin   cargo-rust"] },
    { "cmd": ["echo", "  MISSING  cargo  (required)"] },
    { "cmd": ["echo", "unhealthy: install the tools marked MISSING above"] },
    { "cmd": ["false"] }
  ]
}
